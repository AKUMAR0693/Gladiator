# QA checklist

- `node --check script.js` passes.
- `index.html` loads and size preset buttons switch panel size.
- Mic control buttons do not throw errors in unsupported browsers.
- Missing `OPENAI_API_KEY` shows a clear status message.
- README reflects current controls and startup steps.
